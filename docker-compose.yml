version: '3.9'

services:
  frontend:
    build: .
    command: npm run start
    ports:
      - '3000:3000'
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    restart: always
    # unlessstopped

volumes:
  pgdata:
